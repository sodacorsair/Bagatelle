import * as constants from './constants';

const defaultState = {
    windowWidth: 0,

}

const reducer = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default reducer;